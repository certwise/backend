class Template {
    constructor(template_id, created_date, last_updated, user, items) {
        this.id = id
        this.template_id = template_id
        this.created_date = created_date
        this.last_updated = last_updated
        this.user = user
        this.items = items
    }
}
